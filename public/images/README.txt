Put Ammarah's photos here using these exact filenames:

  ammara1.jpg   -> used on Screen 1 (full-screen background)
  ammara2.jpg   -> used on Screen 2 (left collage photo)
  ammara3.jpg   -> used on Screen 2 (right collage photo)
  ammara4.jpg   -> used on Screen 3 (photo trio, left)
  ammara5.jpg   -> used on Screen 3 (photo trio, center)
  ammara6.jpg   -> used on Screen 3 (photo trio, right)

Until you add real files, each spot shows a soft gold gradient
placeholder instead of a broken image, so the site always looks intentional.

Want different filenames? Edit src/data/birthdayData.js — every
image path lives in one place.
