Drop an MP3 here named exactly:

  birthday.mp3

The floating "Music" button (bottom-right corner) will play/pause it.
If the file isn't there yet, the button simply does nothing when
clicked — nothing breaks.

Want a different filename or path? Edit the src in
src/components/MusicButton.jsx (look for new Audio("/music/birthday.mp3")).
